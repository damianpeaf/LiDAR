[build]
builder = "DOCKERFILE"
dockerfilePath = "services/lidar-server/Dockerfile"

[deploy]
startCommand = "python main.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[environments.production]
rootDirectory = "services/lidar-server"
